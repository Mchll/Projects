//4. Количество нулевых элементов в массиве.
#include <iostream>
#define ll long long
using namespace std;
ll arr[100000], i, ans, n;
int main()
{
	cin >> n;
	for(i = 1; i <= n; ++i) {
		cin >> arr[i];
		if(arr[i] == 0) {
			++ans;
		}
	}
	cout << ans;
	return 0;
}

//4. Количество нулевых элементов в массиве.
#include <iostream>
#define ll long long
using namespace std;
ll a[100000], i, k, n;
int main()
{
	cin >> n;
	for(i = 1; i <= n; ++i) {
		cin >> a[i];
		if(a[i] == 0) {
			++k;
		}
	}
	cout << k;
	return 0;
}
